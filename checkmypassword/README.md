Python program that checks if a password has ever been hacked using Pwned Passwords endpoint with k-Anonymity model.
passwords_to_be_checked.txt file contains the passwords that the user wants to check.
